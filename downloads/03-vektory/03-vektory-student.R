# Занятие 3. Векторы
# Автор: Имя Фамилия
# Группа:

# Выполняйте строки по одной:
# Ctrl + Enter — Windows/Linux
# Cmd + Enter  — macOS


# 1. Создание векторов =============================================

# Вектор хранит упорядоченную последовательность значений.
concentration <- c(12.4, 15.1, 13.8, 14.6)
concentration
length(concentration)

# Даже одно значение — вектор длины 1.
temperature <- 36.7
length(temperature)

# c() объединяет значения и готовые векторы.
sample_group <- c("control", "treatment", "treatment", "control")
quality_check <- c(TRUE, TRUE, FALSE, TRUE)

morning <- c(12.4, 15.1)
evening <- c(13.8, 14.6)
all_measurements <- c(morning, evening)
all_measurements

# Последовательности и повторения.
1:6
6:1

seq(from = 0, to = 20, by = 5)
seq(from = 0, to = 1, length.out = 5)

rep(c("control", "treatment"), times = 3)
rep(c("control", "treatment"), each = 3)


# 2. Тип вектора ===================================================

# Все элементы вектора имеют один общий тип.
typeof(c(1.5, 2.0, 4.2))
typeof(c("control", "sample", "sample"))

is.logical(c(TRUE, FALSE))
is.double(c(1.5, 2.0, 4.2))

# Если типы различаются, R приводит их к общему типу.
c(TRUE, 2)
c(1L, 2.5)
c(TRUE, 2, "sample")

typeof(c(TRUE, 2, "sample"))

# Явное преобразование типа.
as.character(c(1, 2, 3))
as.double(c("1.5", "2.5"))


# Задание 1. План эксперимента -------------------------------------

# В эксперименте используют концентрации 0, 5, 10, 15 и 20 мкмоль/л.
# Каждую концентрацию испытывают на трёх образцах.
#
# 1. Создайте concentration с помощью seq().
# 2. Создайте concentration_repeated, в котором каждая концентрация
#    повторяется три раза.
# 3. Создайте sample_group, в котором по три раза повторяются названия
#    пяти групп: "C0", "C5", "C10", "C15" и "C20".
# 4. Проверьте длину и тип обоих длинных векторов.
# 5. С помощью c() объедините concentration_repeated со строкой
#    "control". Сохраните результат, проверьте его тип и объясните его.

# Напишите код ниже:




# 3. Векторизованные вычисления ====================================

# Операция вектора с одним числом.
signal <- c(10, 20, 30, 40)
signal * 2
signal / 10

# Операция с двумя векторами одинаковой длины.
background <- c(1, 2, 1, 3)
signal - background

# В одной формуле могут сочетаться оба вида операций.
height <- c(1.80, 1.70, 1.75, 1.96)
weight <- c(120, 80, 90, 59)
bmi <- weight / height ^ 2
bmi

round(bmi, digits = 1)
length(bmi)
sum(bmi)
mean(bmi)
median(bmi)
min(bmi)
max(bmi)
range(bmi)


# Ресайклинг -------------------------------------------------------

measurement <- c(10, 20, 30, 40)

# Одно значение повторяется для каждого элемента.
measurement - 2

# Короткий вектор повторяется целиком.
measurement + c(1, 2)

# Несовместимые длины: вычисление выполняется с предупреждением.
measurement + c(1, 2, 3)


# Задание 2. Фоновая поправка --------------------------------------

# Шесть образцов измерили на каналах A, B, A, B, A, B.
# Сигналы: 0.42, 0.51, 0.63, 0.48, 0.70 и 0.59.
# Фон канала A равен 0.04, фон канала B — 0.06.
#
# 1. Сохраните сигналы в signal, а фоновые значения — в background.
# 2. Вычтите соответствующий фон из каждого сигнала и сохраните
#    результат в corrected_signal.
# 3. Умножьте все исправленные сигналы на 10.
# 4. Округлите результаты до одного знака после десятичной точки.
# 5. Рассчитайте среднее и диапазон исправленных сигналов.

# Напишите код ниже:




# 4. Индексирование ================================================

protein_concentration <- c(12.4, 15.1, 13.8, 14.6, 11.9)

# Выбор по положительным индексам.
protein_concentration[1]
protein_concentration[c(2, 4)]
protein_concentration[2:4]

# Индексы определяют набор и порядок элементов.
protein_concentration[c(5, 1, 3)]
protein_concentration[c(2, 2, 4)]

# Отрицательные индексы исключают позиции.
protein_concentration[-3]
protein_concentration[c(-2, -4)]

# Изменение элементов.
measurements <- c(4.2, 5.1, 99, 6.0)
measurements[3] <- 5.4
measurements


# Именованный вектор -----------------------------------------------

height <- c(Anna = 168, Boris = 181, Elena = 174, Pavel = 177)
height

height <- c(168, 181, 174, 177)
names(height) <- c("Anna", "Boris", "Elena", "Pavel")

height[c("Anna", "Pavel")]
height[3]


# Задание 3. Исправляем измерения ---------------------------------

ph <- c(7.1, 6.8, 7.0, 73, 7.2, 6.9)
sample_id <- c("S1", "S2", "S3", "S4", "S5", "S6")

# 1. Извлеките первое, третье и пятое измерения.
# 2. Покажите все измерения, кроме второго.
# 3. Исправьте четвёртое значение: вместо 73 должно быть 7.3.
# 4. Присвойте элементам ph имена из sample_id.
# 5. Извлеките pH образца S5 по имени.
# 6. Найдите максимальный pH.

# Напишите код ниже:




# 5. Сортировка значений ===========================================

signal <- c(0.72, 0.31, 0.56, 0.44, 0.83)

sort(signal)
sort(signal, decreasing = TRUE)
order(signal)

# Индексы из order() можно применить к связанному вектору.
students_height <- c(168, 181, 174, 177)
students_names <- c("Anna", "Boris", "Elena", "Pavel")

students_order <- order(students_height)
students_names[students_order]


# Задание 4. Образцы по возрастанию сигнала ------------------------

sample_id <- c("S1", "S2", "S3", "S4", "S5")
signal <- c(0.72, 0.31, 0.56, 0.44, 0.83)

# Значения на одинаковых позициях относятся к одному образцу.
#
# 1. Отсортируйте сигналы по возрастанию.
# 2. Сохраните индексы, упорядочивающие сигнал, в signal_order.
# 3. Используйте signal_order, чтобы одинаково расположить sample_id
#    и signal.
# 4. Присвойте элементам signal имена из sample_id и отсортируйте
#    именованный вектор по убыванию.
# 5. Найдите максимальный сигнал.

# Напишите код ниже:




# 6. Логические векторы ============================================

temperature <- c(36.4, 37.1, 38.2, 36.8)
temperature >= 37

# Объединение условий.
is_normal <- (temperature >= 36.5) & (temperature <= 37.2)
is_normal

group <- c("control", "treatment", "control", "placebo")
group == "control"


# Логическое индексирование ---------------------------------------

measurements <- c(4.2, 8.1, 3.7, 6.4)
is_high <- measurements >= 5

is_high
measurements[is_high]
measurements[measurements >= 5]

# Одна маска может отбирать связанные элементы из разных векторов.
student_name <- c("Daniel", "Sasha", "Robert", "Alice")
age <- c(15, 32, 18, 8)
is_adult <- age >= 18

student_name[is_adult]
age[is_adult]

# Логическое индексирование слева от присваивания изменяет элементы.
measurements[measurements < 4] <- NA
measurements


# Функции для логических векторов ----------------------------------

quality_check <- c(TRUE, FALSE, TRUE, FALSE, TRUE, TRUE)

sum(quality_check)
mean(quality_check)
any(quality_check)
all(quality_check)

# which() возвращает позиции TRUE.
which(quality_check)


# Задание 5. Контроль качества -------------------------------------

# Допустимая концентрация калибровочного раствора — от 95 до
# 105 мг/л включительно.
sample_id <- c("S1", "S2", "S3", "S4", "S5", "S6")
calibration <- c(98, 107, 101, 94, 105, 99)

# 1. Создайте is_valid, показывающий допустимые измерения.
# 2. Извлеките значения и имена образцов, прошедших контроль.
# 3. Извлеките значения и имена образцов, не прошедших контроль.
# 4. Посчитайте число и долю прошедших контроль образцов.
# 5. Проверьте, прошли ли контроль все образцы и есть ли хотя бы один
#    прошедший.
# 6. Замените не прошедшие контроль значения на NA.

# Напишите код ниже:




# 7. Дополнительные операции =======================================

# Принадлежность набору.
sample_group <- c("control", "treated", "placebo", "treated", "excluded")
allowed_group <- c("control", "placebo")

sample_group %in% allowed_group
sample_group[sample_group %in% allowed_group]

# Повторяющиеся и уникальные значения.
sample_id <- c("S01", "S02", "S03", "S02", "S04", "S01")

unique(sample_id)
duplicated(sample_id)
sample_id[duplicated(sample_id)]

# Пропущенные значения.
measurements <- c(4.2, NA, 5.7, 6.3)

is.na(measurements)
mean(measurements)
mean(measurements, na.rm = TRUE)

# measurements == NA не проверяет наличие пропусков.


# Задание 6. Проверка идентификаторов и измерений ------------------

sample_id <- c("S01", "S02", "S03", "S04", "S03", "S05", "S06", "S02")
concentration <- c(18.2, 21.4, NA, 19.7, 24.1, 17.9, NA, 20.3)

# 1. Получите вектор уникальных идентификаторов.
# 2. Найдите повторные появления идентификаторов.
# 3. С помощью is.na() проверьте, какие измерения пропущены.
# 4. Рассчитайте среднюю концентрацию сначала обычным способом,
#    а затем с na.rm = TRUE. Объясните различие результатов.

# Напишите код ниже:



